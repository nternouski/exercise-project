{
  "name": "shared",
  "version": "1.0.0",
  "description": "código compartido entre backend y frontends",
  "main": "index.js",
  "scripts": {
    "clean": "prettier  --write \"**/*.{ts,html,scss,json}\"",
    "test": "eslint -c .eslintrc.json && ts-mocha -r tsconfig-paths/register -p tsconfig.json test/**/*.spec.ts"
  },
  "dependencies": {
    "eslint": "^7.29.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "typescript": "^4.3.4"
  }
}
